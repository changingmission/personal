@model Tag
<div class="container">
    You are permanently deleting @Model.Title ?
    <form asp-controller="Tags" asp-action="DeleteConfirm" method="post">
        <input name="Id" type="hidden" value="@Model.Id">
        <a asp-action="Index" asp-controller="Tags">Cancel</a>
        <button type="submit" class="btn btn-danger">Confirm Delete</button>
    </form>
</div>